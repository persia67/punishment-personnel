name: Multi-Platform Build & CI

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  multi-platform-build:
    name: Run Multi-Platform Release Pipeline
    uses: ./.github/workflows/release.yml
    permissions:
      contents: write
